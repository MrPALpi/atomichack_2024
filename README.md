## Развертывание проекта
`cp .env.example .env` + настроить перменные
`docker compose up -d`

## Настройка сервиса fast-api
Требования:
 - poetry
Установка зависимостей Python: `poetry install`  
Запуск сервиса: `poerty run start-app`
